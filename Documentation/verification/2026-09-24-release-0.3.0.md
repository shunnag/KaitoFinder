# リリース 0.3.0 (4) の公開結果 — 2026-09-24

対象: KaitoFinder `v0.3.0`（`5bd07bf`、PR #2。tree は `ef6f14e` と同一）、KaitoKit `v0.10.0`（`3f595ad`、PR #34）、
GyoshukuKit `v0.5.0`（`aa10454`、PR #4）。内容は分割アーカイブの編集（M0〜M6、[最終検証](2026-09-23-split-archive-final.md)）。

## 公開前の検証（ロック解除・操作なしの GUI セッション、arm64）

| 検証 | 結果 |
|---|---|
| KaitoKit `swift test`（`f3ee72e`） | 1,412 件（skip 45）+ 互換 34 件、失敗 0。PR の CI は Intel を含め 6/6 成功 |
| GyoshukuKit `swift test`（`b9f413f`、隣接 path 依存） | 245 件（skip 1）、失敗 0 |
| GyoshukuKit tag 参照モード（隣に KaitoKit のない clone、`purge-cache` 後、KaitoKit 0.10.0 = `3f595ad` に解決） | ビルド成功。245 件、skip 1、失敗 2。2 件は `ZipModernMethodEditingTests` が fixture を `../KaitoKit/Tests/Fixtures` から読むテスト側の隣接依存（`e1f16ac` から存在、v0.4.2 にも含まれる）。ライブラリ本体の失敗ではない |
| KaitoFinder `build-for-testing && test-without-building`（`ef6f14e`） | 1,187 件、skip 20、失敗 0。前後とも unlocked。skip は UI ドライバー専用 16、任意の大容量・計測 2、前面化できない QuickLook 2 |
| `verify_ui_integration.py` / `verify_finder_interactions.py` / `verify_preview_sidebar.py` | すべて成功 |

## 公開

| 手順 | 結果 |
|---|---|
| archive / export | Developer ID（`B7A1…EB48`）、`build/ReleaseArchiveDerivedData`、`build/release/ExportOptions.plist`。0.3.0 / build 4、`codesign --verify --deep --strict` 成功 |
| notarize | `build/KaitoFinder.zip`（8,179,611 byte）: id `283ac7db-73c8-4757-8146-0e6e9757d3ff`、**Accepted** |
| staple / Gatekeeper | ユーザーが実行（自動モードの権限判定で拒否されたため）。staple・validate 成功、`spctl`: `source=Notarized Developer ID` |
| 更新ファイル | `prepare_update.py … --output build/updates/0.3.0 --notes Documentation/releases/0.3.0.md --previous-appcast build/updates/0.2.0/appcast.xml`: `KaitoFinder-0.3.0.zip`（8,181,168 byte）、appcast は version 4 / 0.3.0 |
| GitHub Release | 下書き `v0.3.0` に ZIP と appcast.xml を添付 → latest で公開。https://github.com/shunnag/KaitoFinder/releases/tag/v0.3.0 |
| フィード確認 | `releases/latest/download/appcast.xml`: 200（redirect 2 回）、ローカルと byte 一致。enclosure 200、content-length 8,181,168 |
| 実 Sparkle | `Tools/probe_software_update.swift` を本番 Info.plist（HTTPS フィード、`SUPublicEDKey`）の一時 bundle で実行: build 3 から version 4 を検出（errorCode 0）、build 4 からは `SUNoUpdateError`（1001） |

未実施: 0.2.0 (3) から「アップデートを確認…」でのインストール・再起動（GUI 操作）。
追跡: 保存パネルの付属ビューが伸縮中に 6 pt はみ出す一時的なずれが分割の行なしでも起きるか（[最終検証](2026-09-23-split-archive-final.md)）。
