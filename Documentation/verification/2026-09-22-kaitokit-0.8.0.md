# KaitoKit 0.8.0 への追従 — 2026-09-22

## 対象と変更

KaitoFinder `release/0.2.0` を 0.2.0 / build 3 へ更新した。参照先は兄弟 checkout の
KaitoKit `release/0.8.1`（0.8.0 + レビュー修正 R1〜R14）と GyoshukuKit `release/0.4.2`。
兄弟 checkout は読み取りのみ。commit / push、LaunchServices 登録、UI 結合テストは行っていない。

- `ArchiveFormat` の追加 12 case を表示名・文書型・展開サービスへ接続した。
- 文書型は 33 件、識別子の集合は 37 件。新規 import は依頼の列挙に従い **8 件**
  （UDF、WIM、MSI、CHM、ARJ、lzip、Brotli、pbzx）。「6 つ」という表題ではなく列挙された全識別子を採用した。
- DMG と旧 Mac 3 形式は CoreTypes の識別子を参照し、import を追加しない。
  Office / Outlook の拡張子、追加の `.bin` import、拡張子のない Payload の関連付けは追加しない。
  `tlz` は既存の LZMA import に残す。
- 表示名の `lzip` は依頼された小文字表記を採用した。参照 checkout の compat 実装は
  `KaitoArchive.swift:509` で `Lzip` を返すため、この一語は指定値を優先している。
- 展開サービスは文書型の全識別子と一致。一括展開パネルは
  `ArchiveBatchExtractionController.swift:21–35`、ようこそのドロップは
  `WelcomeDropZoneView.swift:30,235` で既に同じ宣言を参照しており、別一覧の修正は不要だった。
- ZIP の Shrink / Reduce 1〜4 / Implode、7z の Zstandard をプレビュー・オープンの許可方式へ追加した。
  ZIP method 7 は引き続き拒否する。
- `.tar.lz` / `.tar.br` は展開できるが編集できない。`ArchiveCapabilities` の拒否名を外側の形式に合わせた。
- README、設計 §5.5、圧縮形式ロードマップ、更新配信の 0.2.0 用の例、リリースノートを更新した。

## AppleDouble の一覧と編集 index

`ReaderOptions.kaitoFinder(password:)` は `appleDoublePolicy: .expose` を使う。
`.merge` は sidecar を除去し resource fork を擬似 entry にして index を詰め直すため、
GyoshukuKit の `.expose` の一覧との間で削除 index と entry 数が一致しなくなる。
全アプリの `ArchiveReader.open` がこの factory を使うことも検索で確認した。
GyoshukuKit の根拠は `ArchiveUpdater.swift:79,182`、`ArchiveRewriter.swift:85`。

`appledouble/finder.zip.b64` は 12 entry。`folder/plain.txt` は原本の index 10 で、
sidecar の後にある。追加したテストではアプリと KaitoKit `.expose` の全 entry を比較し、
`__MACOSX/folder/._rsrc.txt` の存在、`..namedfork` の不在、`mode == .inPlace` と refusal nil を検査する。
その後 `folder/plain.txt` だけを削除・保存し、独立した `.expose` reader で残る 11 entry の順序と
全ファイルの内容が一致することを検査する。**このアプリ経由の XCTest は下記の sandbox 制約により未実行**。

fixture の原本 SHA-256 は `0a7dfb5c9865a4d3ec8441bcf974d6c7d9955bdc92cc30b89accc2e6a6e24599`。
`SHA256SUMS` と Python の ZIP 一覧で上記の原本・項目位置を確認した。

## 追加した項目の拒否規則

根拠のパスはすべて `../KaitoKit/Sources/KaitoKit/Formats/` を起点とする。
一覧の公開情報だけで判定し、能力照会中に stream を開かない。既存の directory / incomplete /
link・特殊項目の拒否は先に適用する。

| 形式 | 一覧での判定 | reader の根拠（行） | fixture による検査 |
|---|---|---|---|
| ARJ | method が stored / compressed most / compressed / compressed faster / no data 以外、または `isEncrypted`、`continuesInNextVolume`、`extendedFilePosition` | `ARJ/ARJReader.swift:200–231` で公開、`:247–265` で読み取り拒否 | `garbled.arj` と `basic.arj` の method 4・volume flag・4 byte の継続位置。変更した基本 header の CRC を再計算 |
| WIM | `formatSpecific["unsupported"]` が存在 | `WIM/WIMReader.swift:235–249,264–270` で EFS・他 part・欠落 resource を公開、`:351` で拒否 | `stored.wim` の metadata / lookup table を一箇所ずつ変え、EFS・他 part・欠落を検査 |
| DMG | `hfsCompressed == "true"`、または委譲先の `unsupported` | `DMG/DMGReader.swift:212–229,256` が decmpfs、`:99–108` が ISO / UDF の一覧をそのまま委譲 | `hfs-zlib.dmg.gz` の `compressed.txt`。委譲先の規則は下記 ISO / UDF の実 entry に `.dmg` を指定しても拒否されることを検査 |
| ISO | `formatSpecific["unsupported"]` が存在 | `ISO/ISORockRidge.swift:135–144` が未対応 ZF、`ISO/ISOReader.swift:239–247` が multi-extent・interleaved 等を公開、`:125` で拒否 | `iso/zisofs-32k.iso.gz` の ZF version 2 と、同じ record を二つの extent に分けた変異。通常 zisofs の許可・SHA も検査 |
| UDF | `formatSpecific["unsupported"]` が存在 | `UDF/UDFFileSystem.swift:156–161,273–277` で未対応 allocation を公開、`:69` で拒否 | `pure150.iso.gz` の readme の FE を ext_ad に変え、tag CRC と checksum を再計算 |
| CHM | `methodDescription == "unknown"` | `CHM/CHMReader.swift:285–311` で未対応 section の非空 file を区別、`:378–379` で拒否 | `basic.chm.gz` の LZXC 署名だけを変更。section 0 のファイルは引き続き読めることも検査 |

以下は項目の規則へ追加しなかった。

- WIM solid / LZMS は `WIM/WIMStructures.swift:118–119` と `WIMReader.swift:42` により一覧前に拒否される。
  `stored.wim` の version / compression flag の変異 2 件で境界を検査する。
- DMG ADC は `DMG/UDIFImage.swift:255–258` で chunk を読む際に拒否される。
  `hfs-adc.dmg.gz` は一覧の解析中に拒否される。個々の file の chunk type は公開 entry にない。
- tar の旧 GNU `S` 型は `Tar/TarReader.swift:185–186`、star sparse は `:192,1090–1096` で
  一覧前に拒否される。Python 標準 tarfile で作る合成 tar 2 件を追加テストに含めた。
- WIM の chunk size、UDIF の chunk の制限など、entry の公開情報から判断できない条件は読み取り時の報告に委ねる。
- CFB と Mac wrapper に、追加すべき「一覧済みの通常 file の未対応方式」規則はなかった。
  AppleDouble merge の reopen 制限は `.expose` では通らない。

7z の追加許可値の根拠は `SevenZip/SevenZipFolderPipeline.swift:638` の `Zstandard`。
ZIP の旧 method 名は `ZIP/ZipReader.swift:2293–2308`。

## fixture の出自と期待値

fixture はすべて既存の `../KaitoKit/Tests/Fixtures/` から base64 を復号し、`.gz.b64` は
KaitoKit 側の既存テストと同じく外側の gzip を展開する。新しい fixture ファイルは保存していない。
期待 SHA は各ディレクトリの `manifest.json` を KaitoKit 側テストが参照する値、
`ISOZisofsTests.swift` の値、および既存 debug `kaito sha` との照合値である。

| ディレクトリ / 採用 fixture | 元データ・生成と照合の記録 |
|---|---|
| `dmg/hfs-zlib.dmg.gz`（拒否は `hfs-adc.dmg.gz` も使用） | macOS hdiutil / HFS+ / ditto で生成。README・manifest に読み取り専用マウントと Windows 系のツールによる照合記録 |
| `udf/pure150.iso.gz` | macOS hdiutil の UDF 1.50。README・manifest と macOS UDF driver での照合記録 |
| `wim/xpress.wim`、`stored.wim` | プロジェクトの writer / XPRESS encoder。README・manifest に Windows 系のツールによる独立展開との照合記録 |
| `cfb/v3.cfb.gz` | 公開仕様からの writer。README・manifest に stream 単位の独立照合記録 |
| `chm/basic.chm.gz` | プロジェクトの ITSF writer / LZX encoder。README・manifest に独立展開との照合記録 |
| `arj/basic.arj`、`garbled.arj`、`nodata.arj` | プロジェクトの writer / lh6 互換 encoder。README・manifest。通常 payload は Windows 系のツール・deark・unar と照合済み |
| `macwrappers/readme.txt.bin` / `.as` / `.hqx` | プロジェクトの生成器。manifest の data fork SHA と lsar / unar の照合記録 |
| `lzip/text.lz`、`bundle.tar.lz` | LZMA stream を囲む生成器と OS の bsdtar。README・manifest に XZ Utils との照合記録 |
| `brotli/text-q1.br`、`bundle.tar.br` | brotli CLI で生成・復号。単体は manifest、tar 内 `a.txt` は既存 `kaito sha` で確認 |
| `pbzx/text.pbzx` | pkgbuild の Payload を調査した生成器で text を 2 XZ chunk に格納。README・manifest に xz との照合記録 |
| `zip-legacy/shrink.zip`、`reduce1.zip`〜`reduce4.zip`、`implode-{4k,8k}-{2trees,3trees}.zip` | プロジェクトの encoder。README・manifest に unzip / deark / Windows 系のツールとの照合範囲を記録 |
| `sevenzip-zstd/zstd-l1.7z` / `zstd-l19.7z` | libarchive の bsdtar で生成。README・manifest に zstd CLI と bsdtar の独立復号との照合記録 |
| `iso/zisofs-32k.iso.gz` | GNU xorriso のイメージ。`ISOZisofsTests.swift:11–18` の原文 SHA |
| `appledouble/finder.zip` | Finder の圧縮と同じ `ditto -c -k --sequesterRsrc --keepParent`。README・generate.sh・SHA256SUMS |

## 実行結果

`CompressionCapabilityTests` に **27 メソッド追加**（既存 15 + 追加 27 = 42）。
新形式 12 件、ARJ no data、ZIP 旧方式 9 fixture、7z Zstandard の 2 水準 × 2 entry、
圧縮 tar 2 形式、Finder ZIP の一覧・編集能力・削除保存、各拒否規則、通常 zisofs、ZIP method 7、
一覧前に拒否される WIM / DMG / tar を含む。`DocumentTypesTests` は既存 6 メソッドの期待値を更新し、
`documents.count == expected.count` の検査を維持した。

| 検証 | 結果 |
|---|---|
| 指定 Debug build | 依存解決で停止、終了コード 74。コンパイル・リンク成功は未確認 |
| 指定 `xcodebuild test` | 依存解決で停止、終了コード 74。XCTest 実行 0 / 失敗 0 / skip 0（起動前停止であり、成功を意味しない） |
| Swift 構文検査 | 変更した Swift 6 ファイルを `swiftc -frontend -parse` で確認、終了コード 0。型検査・ビルド・XCTest の代替ではない |
| plist 構文・宣言の静的照合 | `plutil -lint` 成功。Python で期待値の 3 辞書・表示名・ArchiveFormat 全 32 case・33 文書型・37 サービス識別子・20 import 期待値を照合、すべて一致 |
| 既存 debug `kaito` の補助照合 | プレビュー用テストが対象とする **29 項目の方式名・SHA-256 が一致**。新形式 12 件をすべて含む |
| 既存 debug `kaito` の拒否境界 | **18 ケース一致**（一覧可能・読み取り拒否 13、一覧前の拒否 5）。追加 XCTest と同じ変異フィールドを使用 |
| Finder ZIP の原本確認 | SHA256SUMS、12 entry、sidecar、削除対象の原本 index 10 を確認。アプリによる削除保存は未実行 |
| 差分 | `git diff --check` 成功。変更は依頼範囲の 14 ファイルのみ |

DMG の補助 `kaito sha` は対象の `readme.txt` の SHA が一致する一方、同じ画像の `compressed.txt`
が decmpfs のため全体の終了コードは 1 となる。この既知の拒否を確認しており、画像内の全 file の展開成功とは扱わない。
その他のプレビュー対象 fixture の `sha` は終了コード 0。KaitoFinder の `EntryMaterializer` を
通した成功件数ではない。

### 通常実行したコマンドと sandbox 制約

変更後に以下を実行した。パイプで終了状態を失わないよう、出力全体を一時ログへ保存してから末尾を表示した。

```sh
xcodebuild -project KaitoFinder.xcodeproj -scheme KaitoFinder -destination 'platform=macOS,arch=arm64' \
  -derivedDataPath build/ReviewDerivedData build > /private/tmp/kaitofinder-0.2.0-build.log 2>&1
xcodebuild -project KaitoFinder.xcodeproj -scheme KaitoFinder -destination 'platform=macOS,arch=arm64' \
  -derivedDataPath build/ReviewDerivedData test > /private/tmp/kaitofinder-0.2.0-test.log 2>&1
```

両方とも次の書き込みを sandbox が拒否し、SwiftPM の manifest 解析前後で停止した。

- `/Users/nagash/.cache/clang/ModuleCache/Swift-7JL1KBZ3A6V3.swiftmodule`
- `/Users/nagash/Library/Caches/org.swift.swiftpm/manifests/ManifestLoading/{kaitokit,gyoshukukit,sparkle}.dia`

診断は `Operation not permitted`、続いて `unable to load standard library for target 'arm64-apple-macos14.0'`、
`Could not resolve package dependencies`。アプリの対象 OS を変更したわけではなく、manifest 用のコンパイルである。

許可された一時領域へのキャッシュ変更も、次の **build のみ** で二通り試した。
どちらも manifest の `.dia` の出力先が変わらず、終了コード 74 だった。通常実行の成功として数えていない。

```sh
env CLANG_MODULE_CACHE_PATH=/private/tmp/kaitofinder-module-cache \
  SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/kaitofinder-module-cache \
  xcodebuild -project KaitoFinder.xcodeproj -scheme KaitoFinder -destination 'platform=macOS,arch=arm64' \
  -derivedDataPath build/ReviewDerivedData -packageCachePath /private/tmp/kaitofinder-package-cache build \
  > /private/tmp/kaitofinder-0.2.0-build-caches.log 2>&1

env CLANG_MODULE_CACHE_PATH=/private/tmp/kaitofinder-module-cache \
  SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/kaitofinder-module-cache \
  SWIFTPM_TESTS_PACKAGECACHE=/private/tmp/kaitofinder-package-cache \
  xcodebuild -project KaitoFinder.xcodeproj -scheme KaitoFinder -destination 'platform=macOS,arch=arm64' \
  -derivedDataPath build/ReviewDerivedData -packageCachePath /private/tmp/kaitofinder-package-cache build \
  > /private/tmp/kaitofinder-0.2.0-build-local-cache.log 2>&1
```

補助検証は `../KaitoKit/.build/debug/kaito list <一時領域のfixture>` と `kaito sha <同fixture>` を使用した。
`swift run kaito` は実行していない。構文検査のコマンドは以下。

```sh
xcrun swiftc -frontend -parse \
  KaitoFinder/Model/ArchiveFormatName.swift KaitoFinder/Model/ArchiveReaderOptions.swift \
  KaitoFinder/Model/ArchiveCapabilities.swift KaitoFinder/Model/EntryReadCapability.swift \
  KaitoFinderTests/DocumentTypesTests.swift KaitoFinderTests/CompressionCapabilityTests.swift
plutil -lint KaitoFinder/Info.plist
git diff --check
```

一時的な静的照合・CLI 照合のスクリプトと結果は `/private/tmp/kaitofinder-static-check.py`、
`/private/tmp/kaitofinder-verify-previews.py`、`/private/tmp/kaitofinder-verify-mutations.py`、
`/private/tmp/kaitofinder-verify-rejections.py`、`/private/tmp/kaitofinder-0.2.0-fixture-verification.json` に置いた。
これらは再起動等で失われうる補助資料であり、永続する回帰検査は上記 XCTest に含めている。

## 未検証事項と引き継ぎ

- sandbox の外で、指定の Debug build と `xcodebuild test` 全件を実行する必要がある。
  プレビュー用ファイル作成、Finder ZIP のアプリ経由の削除保存、既存テストとの結合、失敗・既存 skip の件数はその実行で確定する。
- Release ビルド後の LaunchServices 実機登録と開き手の確認はオーケストレータが行う。
- `Tools/verify_ui_integration.py` は依頼どおり未実行。
- DMG 内 ISO / UDF への未対応 metadata の委譲はソースで確認したが、当該 DMG の変異 fixture を使った個別検査は行っていない。

## オーケストレータによる独立検証（2026-09-22 10:0x〜10:3x）

Codex の sandbox では `xcodebuild` が SwiftPM のユーザーキャッシュ書き込みで停止した（上記）。オーケストレータは通常のシェルで
`-derivedDataPath build/ReviewDerivedData` を指定して実行した。参照 checkout は KaitoKit `release/0.8.1`（`85e9a71`）、
GyoshukuKit `release/0.4.2`（`aa24a03`）。

- `xcodebuild … build`: BUILD SUCCEEDED。
- `xcodebuild … test` 全件: **869 件、skip 20、失敗 31 行（12 test case）**。失敗はすべて key window を要する
  `ArchiveDropIntegrationTests`（5）、`ArchivePasswordUITests`（6）、`ArchiveTabTests`（1）、`ArchivePreviewSidebarTests`（1）、
  `ArchiveConflictUITests`（1）で、失敗文は初回リリース時の記録（2026-09-19）と同じ
  「シーンの状態遷移が時間切れ」「expired」「無効なメニュー項目」「NSDragOperation(rawValue: 0)」。
  実行中は同じ Mac で cooViewer の Release ビルド・署名が並行していた。
- 上記 5 class だけを再実行し、テストホスト起動後に `open` で前面化: **41 件、失敗 9**。`ArchiveDropIntegrationTests` の
  実ドラッグ 5 件は通過。残る 9 件（保存パネルのアニメーション 6、タブ 1、プレビューサイドバーのメニュー 1、競合アラートの
  key equivalent 1）は前回も「key window 依存の 9 件」として 3 本の UI ドライバー（`verify_ui_integration.py` /
  `verify_finder_interactions.py` / `verify_preview_sidebar.py`）で別途検証したもの。今回の変更は Model / Info.plist /
  版番号のみで UI コードには触れていないが、ドライバーは実マウス・キー入力を送るため利用者のシェルから
  ロック解除済みの idle な状態で実行する必要があり、**未実行**。
- 今回追加した `CompressionCapabilityTests` 27 件（12 形式のプレビュー SHA-256、ZIP 旧方式 9 fixture、7z Zstandard、
  tar.lz / tar.br の読み取り専用、Finder ZIP の `.expose` 一覧と削除保存、拒否規則）と `DocumentTypesTests` は全件通過。
- LaunchServices の実機登録（`lsregister -f`）と Release ビルドの起動スモークは配布用 archive の作成後に行う。
